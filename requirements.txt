Django>=3.0,<4.0
djangorestframework==3.12.2
pytest-django==4.1.0 # for testing
model-bakery==1.2.1 # for dummy models
pytest-cov==2.10.1 # for coverage
boto3==1.16.34 # for s3 manipultaion
django-storages==1.10.1 # for s3 static and media files
python-dotenv==0.15.0 # for env in .env file
gunicorn==20.0.4 # for serving the django app in prod
psycopg2==2.8.4 # for postgres
djangorestframework==3.12.2 # for api's
django-prometheus==2.1.0 # prometheus to monitoring django behavior
django-cors-headers==3.7.0 # handle cors