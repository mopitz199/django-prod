version: "3.8"
services:
  api:
    build: .
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:${PORT} --settings=${CUSTOM_DJANGO_SETTINGS_MODULE}"
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - .:/code